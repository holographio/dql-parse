{
  "name": "@holographio/dql-parse",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "./src/index.js",
  "gypfile": true,
  "scripts": {
    "build": "npm run build:go && npm run build:addon",
    "build:addon": "node-gyp configure build",
    "build:go": "go mod download && go build -buildmode c-shared -o lib/dql-parse.so src/dql-parse.go"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-addon-api": "^3.1.0"
  },
  "devDependencies": {
    "node-gyp": "^8.0.0"
  }
}
